// 테스트용 배열
const arr = [1,2,3,4,5];
const arr2 = [1,2,3,4];
const number = 1;

// 함수 선언
function binarySearch(arr, low, mid, high, number) {
  
}


// 함수 실행
binarySearch(arr, 0, Math.trunc((0 + arr.length) /2), arr.length, number);
// console.log(Math.trunc((0 + arr.length) /2));


